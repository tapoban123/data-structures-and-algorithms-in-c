#include <stdio.h>

// Solves a significant drawback of Linear queue.
// After deleting values from a queue, we cannot utilise the free space in Linear queue, once a linear queue
// is full it is full forever until we remove all its value.
// Circular queue allows us to utilise the free space.

int main()
{
}